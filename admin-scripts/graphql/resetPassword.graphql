mutation resetPassword($username: String!, $password: String!) {
    update_auth_user(
        where: { username: { _eq: $username } }
        _set: { request_resetpw: $password }
    ) {
        affected_rows
        returning {
            id
            username
        }
    }
}
