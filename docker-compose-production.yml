version: "3"

services:
    db:
        restart: always
        env_file: .env
        networks:
            - 21learning
    runestone:
        restart: always
        ports:
            - "80"
        networks:
            - 21learning
        env_file: .env

        environment:
            WEB2PY_CONFIG: "production"
            WEB2PY_ADMIN_SECURITY_BYPASS: "false"
            RUNESTONE_USE_SERVICES: "true"
            RUNESTONE_USE_HTTPS: "true"
            VIRTUAL_HOST: "${RUNESTONE_HOST}"
            LETSENCRYPT_HOST: "${RUNESTONE_HOST}"
            LETSENCRYPT_EMAIL: "${ADMIN_EMAIL}"
            PGOPTIONS: "-c 'custom.web2py_private_key=${WEB2PY_PRIVATE_KEY}'"
            WEB2PY_PASSWORD: "${WEB2PY_PASSWORD}"
        links:
            - db
        depends_on:
            - "db"

    pgadmin:
        image: dpage/pgadmin4:latest
        restart: always
        networks:
            - 21learning
        links:
            - db
        ports:
            - "80"
        depends_on:
            - "db"
        env_file: .env
        environment:
            PGADMIN_LISTEN_PORT: "80"
            PGADMIN_DEFAULT_EMAIL: "${ADMIN_EMAIL}"
            PGADMIN_DEFAULT_PASSWORD: "${PGADMIN_PASSWORD}"
            VIRTUAL_HOST: "db.${RUNESTONE_HOST}"
            LETSENCRYPT_HOST: "db.${RUNESTONE_HOST}"
            LETSENCRYPT_EMAIL: "${ADMIN_EMAIL}"
        volumes:
            - ./pgadmin/servers.json:/servers.json

    redis:
        restart: always
        networks:
            - 21learning

    dashboard:
        image: "nginx:latest"
        restart: always
        networks:
            - 21learning
        ports:
            - "80"
        env_file: .env
        environment:
            VIRTUAL_HOST: "dashboard.${RUNESTONE_HOST}"
            LETSENCRYPT_HOST: "dashboard.${RUNESTONE_HOST}"
            LETSENCRYPT_EMAIL: "${ADMIN_EMAIL}"
        volumes:
            - ./dashboard/dist/spa:/usr/share/nginx/html

    # new:
    #     image: "nginx:latest"
    #     restart: always
    #     network_mode: "bridge"
    #     ports:
    #         - "80"
    #     env_file: .env
    #     environment:
    #         VIRTUAL_HOST: "new.${RUNESTONE_HOST}"
    #         LETSENCRYPT_HOST: "new.${RUNESTONE_HOST}"
    #         LETSENCRYPT_EMAIL: "${ADMIN_EMAIL}"
    #     volumes:
    #         - ./newfrontend/dist/spa:/usr/share/nginx/html

    hasura:
        restart: always
        ports:
            - "8080"
        networks:
            - 21learning
        env_file: .env
        environment:
            HASURA_GRAPHQL_DATABASE_URL: "postgres://runestone:${POSTGRES_PASSWORD}@db/runestone"
            HASURA_GRAPHQL_ENABLE_CONSOLE: "true" # set to "false" to disable console
            HASURA_GRAPHQL_ENABLED_LOG_TYPES: startup, http-log, webhook-log, websocket-log, query-log
            ## uncomment next line to set an admin secret
            HASURA_GRAPHQL_ADMIN_SECRET: "${HASURA_ADMIN_SECRET_KEY}"
            HASURA_GRAPHQL_JWT_SECRET: "${HASURA_GRAPHQL_JWT_SECRET}"
            VIRTUAL_HOST: "api.${RUNESTONE_HOST}"
            LETSENCRYPT_HOST: "api.${RUNESTONE_HOST}"
            LETSENCRYPT_EMAIL: "cedonner@gmail.com"
            HASURA_GRAPHQL_UNAUTHORIZED_ROLE: anonymous
#  hasura-backend-plus:
#    image: nhost/hasura-backend-plus:latest
#    AUTO_MIGRATE: false
#    restart: always
#    ports:
#      - '3000'
#    environment:
#      SERVER_URL: "https://hbp.${RUNESTONE_HOST}"
#      HASURA_ENDPOINT: http://hasura:8080/v1/graphql
#      HASURA_GRAPHQL_ADMIN_SECRET: '${HASURA_ADMIN_SECRET_KEY}'
#      JWT_ALGORITHM: HS512
#      VIRTUAL_HOST: "hbp.${RUNESTONE_HOST}"
#      LETSENCRYPT_HOST: "hbp.${RUNESTONE_HOST}"
#      LETSENCRYPT_EMAIL: "cedonner@gmail.com"


    # code:
    #     image:  codercom/code-server:latest
    #     restart: always
    #     ports:
    #         - "8080"
    #     networks:
    #         - 21learning
    #     env_file: .env
    #     environment:
    #         VIRTUAL_HOST: "code.${RUNESTONE_HOST}"
    #         LETSENCRYPT_HOST: "code.${RUNESTONE_HOST}"
    #         LETSENCRYPT_EMAIL: "cedonner@gmail.com"
    #         DOCKER_USER: "{$USER}"
    #     volumes:
    #         - ./dashboard/dist/spa:/usr/share/nginx/html

    # # https://coder.com/docs/code-server/install#docker
    #         # -v "$HOME/.local:/home/coder/.local" \
    #         # -v "$HOME/.config:/home/coder/.config" \
    #         # -v "$PWD:/home/coder/project" \
    #         # -u "$(id -u):$(id -g)" \
    #         # -e "DOCKER_USER=$USER" \
    #         # codercom/code-server:latest

networks:
  21learning:
    name: 21learning
    external: true